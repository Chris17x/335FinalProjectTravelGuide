<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View City Response</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>City Information</h1>

    <!-- <p>
        <strong>City Name:</strong> <%= target.cityName %>
    </p>
    <p>
        <strong>Latitude:</strong> <%= target.latitude %>
    </p>
    <p>
        <strong>Longitude:</strong> <%= target.longitude %>
    </p>
    <p>
        <strong>Weather:</strong> <%= weatherReport %>
    </p> -->

    <h2>City Image</h2>
    <% if (imageUrl) { %>
        <img src="<%= imageUrl %>" alt="Image of <%= target.cityName %>" class="city-image">
    <% } else { %>
        <p><em>No image available for this city.</em></p>
    <% } %>

    <p>
        <a href="/">Home</a>
    </p>

</body>
</html>